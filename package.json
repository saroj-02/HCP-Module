{
  "name": "aiova-root-proxy",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install": "npm install --prefix frontend && (python3 -m venv .venv && .venv/bin/pip install -r backend/requirements.txt || pip install -r backend/requirements.txt --break-system-packages)",
    "dev": "npm start",
    "local": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "cd backend && ( [ -f ../.venv/bin/uvicorn ] && ../.venv/bin/uvicorn main:app --host 0.0.0.0 --port $PORT || uvicorn main:app --host 0.0.0.0 --port $PORT )"
  }
}
